.DS_Store
.idea/
node_modules/
.env
.env.*
!.env.example

# Brand kit design assets — 244 MB of source files, and the zips exceed GitHub's 100 MB limit.
# Keep them out of the website repo; distribute via Drive or a dedicated asset repo instead.
Brand Kit/
Brand Kit.zip
Claude outputs/
mvtech_brand_kit_v2/
mvtech_brand_kit/

# Actual on-disk brand kit folder name (the patterns above miss this spelling)
MVtech Brand Kit/
# Nested checkout of the separate outreach repo — must not be committed here
MVtech_Outreach_Ready/
# Internal working notes; would be publicly served by GitHub Pages if committed
SEO-AUDIT-NOTES.md
